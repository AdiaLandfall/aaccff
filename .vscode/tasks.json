{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [

        {
            "label": "Bake - Circle", "command": "mvn clean generate-resources",
            "type": "shell", "group": { "kind": "build" },
            "options": { "cwd": "${workspaceFolder}/circle-build"},
        },

    ]
}